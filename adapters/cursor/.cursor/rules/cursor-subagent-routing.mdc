---
description: Cursor 子 Agent 路由与委派协议
globs:
alwaysApply: true
---

# Cursor 子 Agent 路由

本规则适用于 **Cursor Agent 模式**。子 Agent 通过 **`.cursor/agents/` 项目 subagent** 委派；Task 工具用于内置类型（`explore` / `shell` / `ci-investigator` 等）或与 harness subagent 配合。不使用 `omx`、`spawn_agent`、tmux 或 Codex 专有命令。

## 优先级

1. `harness-kit/core/routing.md` — 路由、阶段门禁、产物规范（强制）
2. 本文件 — Cursor 执行映射
3. 根目录 `AGENTS.md` — Harness 覆盖层有效；OMX/Codex 专章在 Cursor 中**忽略**

平台映射见 `harness-kit/adapters/cursor/orchestration/platform-adapters.zh.md`。

## Harness 强制声明

收到任务后，第一句必须为：

```text
「Harness：<route 或 "小改动，直接处理">」
```

多 task 实现示例：`「Harness：cursor-orchestration:dispatcher-workflow」`

## 阶段门禁（Cursor 强制）

**高于** AGENTS.md 自主性指令。写入阶段产物后须暂停，等用户明确继续。

| 阶段 | 产物路径 | 写入后 | 继续条件（用户须在本会话明确说过） |
| --- | --- | --- | --- |
| 设计 | `.ai-runtime-artifacts/specs/` | **停止**，不进入 plan 或改业务代码 | 「写计划」「制定实施计划」「直接实现」「直接做」 |
| 计划 | `.ai-runtime-artifacts/plans/` | **停止**，不派发 implementer 或改业务代码 | 「开始实现」「执行」「并行执行」 |
| 决策 | `.ai-runtime-artifacts/decisions/` | **停止** | 「执行」或用户指定下一步 |

**「已批准 plan/spec」** = 用户在本会话说过上表「继续条件」之一，或用户在本轮任务开头一次性授权该阶段（如「spec 确认后直接写计划」）。

**用户说「之后都默认你推荐的就好」** = 仅跳过方案**选择**讨论；**不跳过** spec/plan 写入后的审查暂停，除非用户同时说「spec/plan 也不用等我确认」。

**实现阶段**：收到「开始实现」后，Leader **必须**委派 `.cursor/agents/harness-implementer`，禁止主 Agent 在主线程直接修改业务代码（routing「小改动」除外）。

暂停时回复须包含：产物路径、摘要、以及 artifact 模板 `## Next` 中的选项。

## 项目 Subagent（`.cursor/agents/`）

| 文件 | 角色 | readonly |
| --- | --- | --- |
| `harness-implementer.md` | 有界 WU 实现 | false |
| `harness-reviewer.md` | 独立审查 | true |
| `harness-explorer.md` | 只读探查 | true |
| `harness-debugger.md` | 缺陷调查 | 视阶段 |

委派示例：`Use the harness-implementer subagent to implement WU-01: ...`

详细 prompt 与返回格式见各 agent 文件及 `harness-kit/adapters/cursor/orchestration/agents/`。

## Task 内置类型（补充）

| 任务形态 | 机制 | 说明 |
| --- | --- | --- |
| 有界实现 / 审查 | **`.cursor/agents/harness-*`** | 优先于 Task `generalPurpose` |
| 仓库搜索（无 harness 角色时） | Task `explore` | `readonly: true` |
| Shell / 测试 / 构建 | Task `shell` | 仅执行命令 |
| CI 失败 | Task `ci-investigator` | 只读分析 |

## Leader 协议

1. 路由判定 → 声明 `「Harness：…」`
2. 遵守**阶段门禁**；非小改动读 `harness-kit/core/routing.md` 与相关 spec/plan
3. 实现阶段委派 `harness-implementer`；完成后委派 **独立** `harness-reviewer`
4. 并行 WU ≤5（默认 3）；维护 `tracking/DISPATCH-TRACK-*.md`
5. 整合子 Agent 结果，主 Agent 验证后再声称完成

## 何时委派 vs 主 Agent 直接做

**委派 subagent：**

- 已批准 plan 中的 WU（实现 → `harness-implementer`）
- 实现后的审查（→ `harness-reviewer`，不同实例）
- 跨 3+ 模块只读探索（→ `harness-explorer`）
- 缺陷调查（→ `harness-debugger`）

**主 Agent 直接做：**

- routing「小改动」（单文件机械修改）
- 写 spec / plan / decision 产物（写完后**暂停**）
- 范围不清晰（先 brainstorming）

## 推荐 skill 链

```text
superpowers:brainstorming → [门禁] → superpowers:writing-plans → [门禁] → cursor-orchestration → superpowers:verification-before-completion
```

并行编排详情：`.agents/skills/cursor-orchestration/SKILL.md` → `orchestration/dispatcher-workflow.md`。

## Git（Leader）

- 组织规范：**invoke `git-xywh` skill**（见 `harness-kit/core/routing.md` Git 路由行）
- 项目差异：`harness-kit/project.git.md`
- **Leader** 执行 commit / push / MR；子 Agent 默认禁止（见各 `harness-*.md`）
- 声明示例：`「Harness：git-xywh + project.git.md」`

## 禁止

- 未过阶段门禁修改业务代码
- 主 Agent 在实现阶段直接改代码（非小改动）
- 实现与审查同一 subagent 实例
- 调用 omx CLI；无验证证据声称完成
