---
description: Cursor 子 Agent 路由与委派协议
globs:
alwaysApply: true
---

# Cursor 子 Agent 路由

本规则适用于 **Cursor Agent 模式**。子 Agent 通过 **`.cursor/agents/` 项目 subagent** 委派；Task 工具用于内置类型（`explore` / `shell` / `ci-investigator` 等）或与 harness subagent 配合。不使用 `omx`、`spawn_agent`、tmux 或 Codex 专有命令。

## 优先级

1. `harness-kit/core/routing.md` — 路由、阶段门禁、产物、按判定加载（强制）
2. 本文件 — Cursor 委派映射
3. 根目录 `AGENTS.md` — Harness 覆盖层有效；OMX/Codex 专章在 Cursor 中**忽略**

平台映射：`harness-kit/adapters/cursor/orchestration/platform-adapters.zh.md`。

## Harness 强制声明

收到任务后，第一句必须为：

```text
「Harness：<route 或 "小改动，直接处理">」
Skills: <slug>@<path> loaded|skipped   # 本阶段有 route skill 时
```

多 task 实现示例：`「Harness：cursor-orchestration:dispatcher-workflow」`

## 阶段门禁（Cursor 强制）

**高于** AGENTS.md 自主性指令。完整表格、已批准判定与暂停回复格式见 **`harness-kit/core/routing.md` § 阶段门禁**。

**Cursor 追加：** 「开始实现」后先 **WORKTREE-INIT**（`dispatcher-workflow.md` §0），再按 `wu_type` 委派；派发含 `worktree_path`，子 Agent 仅在该目录改代码。主 Agent 禁止写业务代码（「小改动」除外）。

## 项目 Subagent 与 Task 补充

角色映射、readonly、Task 类型见 **`platform-adapters.zh.md` § Cursor 角色映射**。

委派示例：`Use the harness-coder subagent to implement WU-01: ...` / `Use the harness-implementer subagent for WU-02: ...`

详细 prompt 与返回格式：各 `.cursor/agents/harness-*.md`（薄壳）→ `orchestration/agents/*.md`（正文）。

## 沟通语言

对用户回复、阶段门禁说明、子 Agent 派发 prompt 与整合反馈均使用**中文**（细则见 **`harness-kit/core/routing.md` § 沟通语言**）。代码标识符、路径、固定段键名（如 `### Skills 使用`）可保留英文。

## Leader 要点

完整职责、阶段链、汇报规范见 **`orchestration/agents/leader.md`**。此处仅列 Cursor 硬约束：

1. 路由判定 → 声明 `「Harness：…」`；遵守 **阶段门禁**
2. WORKTREE-INIT → 派发 WU（含 `worktree_path`）；`auto` 解析 SKILL 路径；无 `### Skills 使用` 不整合；并行 ≤5；维护 DISPATCH-TRACK
3. **GROUP 尾盘**：先集体测试（Write `*-collective-test.md`）→ 再集体审查（委派 `harness-reviewer`，Leader Write `*-code-review.md`）；未过禁止声称批次完成
4. 整合子 Agent 结果后进入尾盘；`code_review: PASS` 不替代集体审查；可按 spec 合法 `SKIPPED`
5. 多 task 编排：激活 `cursor-orchestration` skill 后 Read `dispatcher-workflow.md`（勿在会话开始预读）
6. 文档审查：Load `document-review`；**代码**审查用 `requesting-code-review`（非 document-review）

## 何时委派 vs 主 Agent 直接做

**委派 subagent：**

- 已批准 plan 中的 WU（代码 → `harness-coder`；docs/chore/config → `harness-implementer`；测试 / E2E → `harness-test-engineer`）
- 实现后的审查（→ `harness-reviewer`，不同实例）
- 跨 3+ 模块只读探索（→ `harness-explorer`）
- 缺陷调查（→ `harness-debugger`）
- 信息调研 / 网页搜索 / 截图取证（→ `harness-web-investigator`）

**主 Agent 直接做：**

- routing「小改动」（单文件机械修改）
- 写 spec / plan / decision 产物（写完后**暂停**）
- 范围不清晰（先 brainstorming）

## Git（Leader）

见 **`harness-kit/core/routing.md` § Git 协作** 与 **`core/runbooks.md` § Git 协作**。子 Agent 默认不 commit/push。

## 禁止

- 未过阶段门禁修改业务代码
- 主 Agent 在实现阶段直接改代码（非小改动）
- 实现与审查同一 subagent 实例
- 调用 omx CLI；无验证证据声称完成
- 跳过尾盘集体测试或集体审查（routing「小改动」除外）
- 未 Write `collective-test` / `code-review` 产物即写 execution-log「批次完成」
