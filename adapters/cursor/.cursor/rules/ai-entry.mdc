---
description: 统一 AI 入口
globs:
alwaysApply: true
---

# Cursor 统一入口

## 规范优先级

1. `harness-kit/core/` — 路由、产物、验证（强制）
2. `.cursor/rules/cursor-subagent-routing.mdc` — Cursor 子 Agent 委派
3. 根目录 `AGENTS.md` — Harness 覆盖层有效；**OMX/Codex 专章在 Cursor 中忽略**（omx、tmux、spawn_agent）

## 会话开始

1. 读取 `harness-kit/core/routing.md`
2. 多 task 实现走 `cursor-orchestration`（`.agents/skills/cursor-orchestration/`）
3. 可选深读：`harness-kit/entrypoints/AGENTS.cursor-overlay.md`

## 声明

每个任务第一句：`「Harness：<route 或 "小改动，直接处理">」`
