---
description: 统一 AI 入口
globs:
alwaysApply: true
---

# Cursor 统一入口

## 规范优先级

1. `harness-kit/core/routing.md` — 路由、阶段门禁、按判定加载（强制）
2. `.cursor/rules/cursor-subagent-routing.mdc` — subagent 委派
3. 根目录 `AGENTS.md` — Harness 覆盖层有效；OMX 专章忽略

## 每任务（必做）

1. 首句：`「Harness：<route 或 "小改动，直接处理">」`；本阶段有 route skill → 次行 `Skills:`，先 Load 再交付（`routing.md` § 阶段指定 skill 必用）
2. **沟通语言：** 对用户回复与子 Agent 派发/整合使用**中文**（`routing.md` § 沟通语言）
3. **非小改动**：Read `harness-kit/core/routing.md`，按 § 按判定加载 追加文件（禁止跳过路由表）

## 按 routing 判定加载（勿在会话开始预读）

| 判定 | 再读 |
| --- | --- |
| 多 task / 已批准 plan | `cursor-orchestration` skill → `orchestration/dispatcher-workflow.md` |
| 派发 WU（`wu_skills: auto`） | `orchestration/skill-preferences.zh.md` |
| 写 spec / plan / decision | `core/artifacts.md` + 对应 `artifact-templates/` |
| Git | `git-xywh` + `project.git.md` + `core/runbooks.md` § Git 协作 |
| 改代码 / 验证 | `project.profile.md`、`context-map.md`（涉及模块时）、`project.verification.md`、`core/verification.md` |

委派与禁止项：见 `cursor-subagent-routing.mdc`（本文件不重复）。
