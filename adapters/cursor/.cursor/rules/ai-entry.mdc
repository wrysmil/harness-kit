---
description: 统一 AI 入口
globs:
alwaysApply: true
---

# Cursor 统一入口

## 规范优先级

1. `harness-kit/core/routing.md` — 路由、**阶段门禁**、产物（强制）
2. `.cursor/rules/cursor-subagent-routing.mdc` — subagent 委派
3. 根目录 `AGENTS.md` — Harness 覆盖层有效；OMX 专章忽略

## 会话开始

1. 读取 `harness-kit/core/routing.md`（含阶段门禁）
2. 多 task 实现：`.cursor/agents/harness-implementer` + `cursor-orchestration` skill
3. Git 任务：**先 invoke / Read `git-xywh` skill**，再读 `project.git.md`；步骤见 `harness-kit/core/runbooks.md` § Git 协作
4. 编排步骤：`harness-kit/adapters/cursor/orchestration/dispatcher-workflow.md`

## 声明

每个任务第一句：`「Harness：<route 或 "小改动，直接处理">」`
